/* a comment */
let
	var N := 8
	type intArray = array of int
	var row := intArray [ N ] of 0
	
	function printboard() =
		(for i := 0 to N-1 /* something */
			do print(if col[i]=j then " 0" else " .");
		print("\n"));

	in try(0, 1);
end
